import React from 'react'
import styles from './footer.module.css'

const Footer = (props) => {
  return (
    <footer className={styles.footer}>
      <p className={styles.title}>Make Your Business Cards!</p>
    </footer>
  )
}

export default Footer
